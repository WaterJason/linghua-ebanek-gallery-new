# 聆花文化ERP系统开发文档

## 项目概述

聆花文化ERP系统是一个集产品管理、库存管理、采购管理、销售管理、排班管理、制作管理和渠道管理于一体的综合管理系统。系统采用最佳实践的编程方式，追求代码量少、低耦合、高复用性和易于排查的实现方案。

## 核心需求

1. **排班模块**：管理员工排班和出勤记录
2. **产品管理模块**：支持产品图片可视化，粘贴上传图片，导入导出功能
3. **多仓库管理**：实时预览库存，产品图片可视化
4. **采购模块**：基础的采购功能
5. **销售模块**：支持订单和POS销售两种模式，POS销售可通过关键词搜索产品
6. **渠道管理**：支持不同渠道设置不同销售价格
7. **制作管理模块**：按件计算制作工资
8. **咖啡店销售记录**：记录每日销售数据

## 技术架构

### 前端技术栈
- Next.js 15.2.4
- React 19
- Tailwind CSS
- Radix UI组件库
- React Hook Form表单管理
- Zod数据验证

### 后端技术栈
- Next.js API Routes
- Prisma ORM
- PostgreSQL数据库
- NextAuth认证

### 开发原则
- 领域驱动设计(DDD)
- SOLID原则
- 微服务思想
- 组件化和模块化
- 测试驱动开发

## 系统架构设计

### 代码组织结构

```
/app
  /api                 # API路由
    /products          # 产品相关API
    /inventory         # 库存相关API
    /sales             # 销售相关API
    ...
  /modules             # 业务模块x
    /product           # 产品模块
    /inventory         # 库存模块
    /sales             # 销售模块
    /schedule          # 排班模块
    ...
/components
  /ui                  # 基础UI组件
  /forms               # 表单组件
  /layouts             # 布局组件
  /modules             # 模块特定组件
/lib
  /hooks               # 自定义Hooks
  /utils               # 工具函数
  /services            # 服务层
  /validators          # 数据验证
/prisma                # 数据库模型
```

### 数据库设计

系统使用PostgreSQL数据库，通过Prisma ORM进行管理。主要数据模型包括：

1. **User**: 用户信息和认证
2. **Employee**: 员工信息
3. **Product**: 产品信息
4. **Warehouse**: 仓库信息
5. **InventoryItem**: 库存项
6. **Supplier**: 供应商信息
7. **PurchaseOrder**: 采购订单
8. **Schedule**: 排班信息
9. **Sale**: 销售订单
10. **SaleItem**: 销售项目
11. **Channel**: 销售渠道
12. **ChannelPrice**: 渠道价格
13. **Production**: 制作工单
14. **CoffeeShopSale**: 咖啡店销售

## 模块详细设计

### 1. 排班模块

**功能描述**：
- 创建员工排班表
- 记录实际出勤情况
- 自动计算日薪
- 批量排班
- 排班模板
- 排班冲突检测

**技术实现**：
- 扩展现有的`Schedule`模型
- 创建排班管理界面
- 实现日历视图和列表视图
- 开发排班统计功能
- 实现批量排班和排班模板

**完成小结**：
- 创建了完整的排班管理界面，支持日历视图、列表视图和统计视图
- 实现了单个排班的添加、编辑和删除功能
- 开发了批量排班功能，支持按日期范围和星期批量创建排班
- 实现了排班模板管理，可保存和应用常用排班模式
- 添加了排班冲突检测功能，自动识别时间重叠的排班
- 开发了排班统计功能，包括员工工时统计、每日排班统计和排班分布分析
- 实现了排班数据导出功能，支持Excel格式
- 优化了移动端体验，提供响应式界面
- 添加了月视图和周视图切换功能

**关键代码示例**：
```typescript
// 排班服务
export class ScheduleService {
  async createSchedule(employeeId: number, date: Date, startTime: string, endTime: string): Promise<Schedule> {
    return prisma.schedule.create({
      data: {
        employeeId,
        date,
        startTime,
        endTime
      }
    });
  }

  async createBatchSchedules(scheduleRequests: ScheduleRequest[]): Promise<Schedule[]> {
    const createdSchedules = [];

    for (const request of scheduleRequests) {
      const schedule = await this.createSchedule(
        request.employeeId,
        request.date,
        request.startTime,
        request.endTime
      );
      createdSchedules.push(schedule);
    }

    return createdSchedules;
  }

  async calculateDailySalary(employeeId: number, date: Date): Promise<number> {
    const employee = await prisma.employee.findUnique({
      where: { id: employeeId }
    });

    if (!employee) throw new Error('员工不存在');

    return employee.dailySalary;
  }

  async detectScheduleConflicts(startDate: Date, endDate: Date): Promise<ScheduleConflict[]> {
    // 实现排班冲突检测逻辑
    // 返回冲突的排班列表
  }
}
```

### 2. 产品管理模块

**功能描述**：
- 产品信息管理
- 产品图片上传（支持粘贴）
- 产品分类管理
- 多级价格管理
- 产品导入导出

**技术实现**：
- 扩展`Product`模型
- 实现图片上传组件
- 开发Excel导入导出功能

**关键代码示例**：
```tsx
// 图片粘贴上传组件
export function ImagePasteUpload({ onImageUploaded }: { onImageUploaded: (url: string) => void }) {
  const handlePaste = useCallback(async (e: ClipboardEvent) => {
    const items = e.clipboardData?.items;
    if (!items) return;

    for (let i = 0; i < items.length; i++) {
      if (items[i].type.indexOf('image') !== -1) {
        const blob = items[i].getAsFile();
        if (!blob) continue;

        // 上传图片
        const formData = new FormData();
        formData.append('file', blob);

        try {
          const response = await fetch('/api/upload', {
            method: 'POST',
            body: formData,
          });

          if (!response.ok) throw new Error('上传失败');

          const { url } = await response.json();
          onImageUploaded(url);
        } catch (error) {
          console.error('图片上传错误:', error);
        }
      }
    }
  }, [onImageUploaded]);

  // 组件实现...
}
```

### 3. 多仓库管理

**功能描述**：
- 多仓库设置
- 库存实时查询
- 库存移动管理
- 库存预警功能

**技术实现**：
- 创建`Warehouse`和`InventoryItem`模型
- 实现库存管理界面
- 使用命令模式处理库存操作

**关键代码示例**：
```typescript
// 库存命令
interface InventoryCommand {
  execute(): Promise<void>;
  undo?(): Promise<void>;
}

// 库存转移命令
export class StockTransferCommand implements InventoryCommand {
  constructor(
    private sourceWarehouseId: number,
    private targetWarehouseId: number,
    private productId: number,
    private quantity: number,
    private inventoryRepository: InventoryRepository
  ) {}

  async execute(): Promise<void> {
    // 实现库存转移逻辑
  }

  async undo(): Promise<void> {
    // 实现撤销逻辑
  }
}
```

### 4. 采购模块

**功能描述**：
- 供应商管理
- 采购订单创建和跟踪
- 采购入库处理
- 采购付款状态记录

**技术实现**：
- 创建`Supplier`和`PurchaseOrder`模型
- 开发采购订单界面
- 实现与库存模块的集成

### 5. 销售模块

**功能描述**：
- 订单销售模式
- POS销售模式
- 销售统计和报表

**技术实现**：
- 扩展`GallerySale`模型
- 创建POS界面
- 使用状态机管理销售流程

**关键代码示例**：
```typescript
// POS销售状态机
export const posSaleMachine = createMachine({
  id: 'posSale',
  initial: 'idle',
  context: {
    items: [],
    customer: null,
    paymentMethod: null,
    total: 0,
    error: null
  },
  states: {
    idle: {
      on: {
        ADD_ITEM: {
          target: 'editing',
          actions: 'addItemToCart'
        }
      }
    },
    // 其他状态...
  }
});
```

### 6. 渠道管理

**功能描述**：
- 渠道信息管理
- 渠道特定价格设置
- 渠道库存跟踪
- 渠道销售统计

**技术实现**：
- 创建`Channel`和`ChannelPrice`模型
- 在产品管理中添加渠道价格设置
- 开发渠道管理界面

### 7. 制作管理模块

**功能描述**：
- 制作工单管理
- 按件计费标准设置
- 制作完成记录
- 自动计算制作工资

**技术实现**：
- 扩展`PieceWork`和`PieceWorkDetail`模型
- 创建制作工单界面
- 实现工资自动计算功能

### 8. 咖啡店销售记录

**功能描述**：
- 咖啡店每日销售记录
- 销售员关联
- 销售统计和提成计算

**技术实现**：
- 扩展`CoffeeShopSale`和`CoffeeShopShift`模型
- 创建咖啡店销售记录界面
- 实现与员工提成的集成

**完成小结**：
- 已完成咖啡店销售表单组件，支持记录详细销售商品明细
- 实现了支付方式分类记录（现金、刷卡、微信、支付宝、其他）
- 添加了顾客数量统计功能
- 创建了咖啡店销售报表和商品分析报表
- 实现了报表导出功能

### 9. 综合报表系统

**功能描述**：
- 整合所有模块的报表功能
- 提供多维度数据分析
- 支持数据可视化展示
- 支持报表导出功能

**技术实现**：
- 创建统一的报表页面
- 使用Recharts实现数据可视化
- 实现报表导出API
- 集成各模块数据源

**完成小结**：
- 已完成综合报表系统的基础架构
- 实现了销售报表仪表盘，支持珐琅馆和咖啡店销售数据分析
- 实现了库存报表仪表盘，支持库存状态和低库存预警分析
- 实现了生产报表仪表盘，支持生产趋势和员工绩效分析
- 实现了财务报表仪表盘，支持收入、支出和利润分析
- 添加了报表导出功能，支持Excel格式导出
- 所有报表支持日期范围筛选，部分报表支持员工和仓库筛选

### 10. UI/UX和可视化增强

**功能描述**：
- 优化用户界面，提供更好的用户体验
- 增强数据可视化展示
- 优化移动端适配
- 提供更丰富的交互元素

**技术实现**：
- 创建响应式组件
- 优化移动端布局
- 增强图表和数据展示
- 实现数据导入导出功能

**完成小结**：
- 创建了移动端优化的仪表盘组件（MobileDashboardCard）
- 实现了响应式数据网格组件（ResponsiveDataGrid）
- 开发了高级图表组件（EnhancedChart），支持多种图表类型和交互
- 实现了通用数据导入导出组件（DataImportExport）
- 创建了移动端和桌面端优化的仪表盘页面
- 添加了移动端检测钩子（useIsMobile）
- 优化了组件在不同设备上的显示效果
- 实现了数据可视化的交互增强，如图表类型切换、数据筛选等
- 添加了图表导出功能

### 11. 产品管理模块

**功能描述**：
- 产品信息管理
- 产品分类管理
- 产品价格设置
- 产品图片上传

**技术实现**：
- 创建产品管理界面
- 实现产品CRUD操作
- 实现产品分类管理
- 实现产品图片上传功能

**完成小结**：
- 创建了产品管理页面，支持产品列表和详情查看
- 实现了产品添加、编辑和删除功能
- 开发了产品分类管理功能
- 实现了产品图片上传和预览功能
- 添加了产品数据导入导出功能
- 集成了响应式数据网格，优化了移动端体验

### 12. 权限管理模块

**功能描述**：
- 角色管理
- 权限分配
- 用户角色管理
- 权限控制

**技术实现**：
- 创建角色管理界面
- 实现权限分配功能
- 实现用户角色管理
- 实现基于角色的访问控制

**完成小结**：
- 创建了权限管理页面，包括角色管理和用户角色分配
- 实现了角色添加、编辑和删除功能
- 开发了细粒度的权限分配功能，支持模块级权限控制
- 实现了用户角色分配功能
- 设计了基于角色的访问控制系统
- 优化了权限管理的用户界面，提供直观的权限设置体验

# 聆花文化ERP系统详细开发计划

## 项目管理方法论

本项目采用**敏捷开发方法**结合**精益开发原则**，通过短迭代周期（Sprint）快速交付有价值的功能，同时持续优化开发流程，减少浪费。

### 敏捷开发框架
- **迭代周期**：2周一个Sprint
- **每日站会**：15分钟，同步进度、解决阻碍
- **Sprint计划会**：每个Sprint开始时确定目标和任务
- **Sprint回顾会**：每个Sprint结束时总结经验教训
- **持续集成/持续部署**：确保代码质量和快速反馈

### 项目角色与职责
- **产品负责人**：确定产品需求优先级，验收功能
- **技术负责人**：技术架构设计，确保代码质量
- **开发团队**：实现功能，编写测试，修复缺陷
- **测试工程师**：设计测试用例，执行测试，报告缺陷
- **DevOps工程师**：负责CI/CD流程，环境配置

## 详细开发时间表

### 第0阶段：项目准备与规划（1周）

#### 第1天：项目启动
- 召开项目启动会议
- 确认项目目标和范围
- 建立项目沟通渠道（如Slack、Jira）

#### 第2-3天：需求分析与规划
- 详细分析业务需求
- 创建用户故事和验收标准
- 建立产品待办事项列表（Product Backlog）

#### 第4-5天：技术准备
- 搭建开发环境
- 配置版本控制系统
- 设置CI/CD流水线
- 确定编码规范和代码审查流程

### 第1阶段：数据库设计和基础架构（2周）

#### Sprint 1（第1-2周）

##### 第1周：数据库设计
- **第1-2天**：设计数据库模型
  - 扩展现有的User和Employee模型
  - 设计Product和相关模型（含图片存储）
  - 设计Warehouse和InventoryItem模型
  - 设计销售相关模型（Sale, SaleItem）

- **第3-4天**：实现数据库迁移
  - 创建Prisma迁移文件
  - 编写数据库种子脚本
  - 实现数据库版本控制

- **第5天**：数据访问层设计
  - 实现Repository模式
  - 设计数据传输对象（DTO）
  - 编写数据验证逻辑

##### 第2周：基础架构搭建
- **第1-2天**：API基础结构
  - 设计RESTful API规范
  - 实现API错误处理中间件
  - 设计API认证和授权机制

- **第3-4天**：前端基础架构
  - 创建组件库基础
  - 实现主题和样式系统
  - 设计状态管理策略

- **第5天**：Sprint回顾与演示
  - 演示已完成的基础架构
  - 回顾第一个Sprint
  - 规划下一个Sprint

### 第2阶段：核心模块开发（6周）

#### Sprint 2（第3-4周）：产品管理模块

##### 第3周：产品基础功能
- **第1-2天**：产品数据模型实现
  - 完善Product模型
  - 实现产品分类功能
  - 设计多级价格结构

- **第3-5天**：产品管理API
  - 实现产品CRUD操作
  - 开发产品搜索功能
  - 实现产品导入导出API

##### 第4周：产品UI和图片功能
- **第1-3天**：产品管理界面
  - 实现产品列表和详情页
  - 开发产品编辑表单
  - 创建产品分类管理界面

- **第4天**：图片上传功能
  - 实现图片上传组件
  - 开发粘贴上传功能
  - 实现图片预览和裁剪

- **第5天**：Sprint回顾与演示
  - 演示产品管理模块
  - 收集反馈并调整
  - 规划下一个Sprint

#### Sprint 3（第5-6周）：库存管理模块

##### 第5周：库存基础功能
- **第1-2天**：库存数据模型实现
  - 完善Warehouse和InventoryItem模型
  - 实现库存事务记录
  - 设计库存预警机制

- **第3-5天**：库存管理API
  - 实现库存查询API
  - 开发库存移动API（入库、出库、调拨）
  - 实现库存报表API

##### 第6周：库存UI和业务逻辑
- **第1-3天**：库存管理界面
  - 实现多仓库视图
  - 开发库存操作界面
  - 创建库存报表界面

- **第4天**：库存业务逻辑
  - 实现命令模式处理库存操作
  - 开发库存预警通知
  - 实现库存历史记录

- **第5天**：Sprint回顾与演示
  - 演示库存管理模块
  - 收集反馈并调整
  - 规划下一个Sprint

#### Sprint 4（第7-8周）：销售模块

##### 第7周：销售基础功能
- **第1-2天**：销售数据模型实现
  - 完善Sale和SaleItem模型
  - 实现支付方式和订单状态
  - 设计销售统计结构

- **第3-5天**：销售管理API
  - 实现订单创建和管理API
  - 开发POS销售API
  - 实现销售报表API

##### 第8周：销售UI和业务逻辑
- **第1-2天**：订单销售界面
  - 实现订单创建和管理界面
  - 开发客户管理功能
  - 创建订单跟踪界面

- **第3-4天**：POS销售界面
  - 实现产品搜索和选择
  - 开发购物车功能
  - 创建结账流程

- **第5天**：Sprint回顾与演示
  - 演示销售模块
  - 收集反馈并调整
  - 规划下一个Sprint

### 第3阶段：辅助模块开发（6周）

#### Sprint 5（第9-10周）：排班和采购模块

##### 第9周：排班模块
- **第1-2天**：排班数据模型实现
  - 完善Schedule模型
  - 实现出勤记录功能
  - 设计薪资计算规则

- **第3-5天**：排班功能实现
  - 开发排班管理界面
  - 实现日历视图
  - 创建薪资计算功能

##### 第10周：采购模块
- **第1-2天**：采购数据模型实现
  - 完善Supplier和PurchaseOrder模型
  - 实现采购状态跟踪
  - 设计与库存的集成点

- **第3-4天**：采购功能实现
  - 开发供应商管理界面
  - 实现采购订单创建和跟踪
  - 创建采购入库流程

- **第5天**：Sprint回顾与演示
  - 演示排班和采购模块
  - 收集反馈并调整
  - 规划下一个Sprint

#### Sprint 6（第11-12周）：渠道和制作管理模块

##### 第11周：渠道管理模块
- **第1-2天**：渠道数据模型实现
  - 完善Channel和ChannelPrice模型
  - 实现渠道特定价格功能
  - 设计渠道库存跟踪

- **第3-5天**：渠道功能实现
  - 开发渠道管理界面
  - 实现渠道价格设置
  - 创建渠道销售报表

##### 第12周：制作管理模块
- **第1-2天**：制作数据模型实现
  - 完善Production和相关模型
  - 实现计件工资计算
  - 设计工单跟踪功能

- **第3-4天**：制作功能实现
  - 开发工单管理界面
  - 实现计件工资设置
  - 创建工资计算报表

- **第5天**：Sprint回顾与演示
  - 演示渠道和制作管理模块
  - 收集反馈并调整
  - 规划下一个Sprint

#### Sprint 7（第13-14周）：咖啡店销售和系统集成

##### 第13周：咖啡店销售模块
- **第1-2天**：咖啡店数据模型实现
  - 完善CoffeeShopSale模型
  - 实现销售员关联
  - 设计提成计算规则

- **第3-5天**：咖啡店功能实现
  - 开发销售记录界面
  - 实现提成计算
  - 创建销售统计报表

##### 第14周：系统集成和优化
- **第1-3天**：模块间集成
  - 确保所有模块正常协同工作
  - 优化跨模块业务流程
  - 实现统一的数据导出功能

- **第4-5天**：性能优化
  - 识别并解决性能瓶颈
  - 优化数据库查询
  - 实现前端性能优化
  - Sprint回顾与演示

### 第4阶段：测试、部署和上线（4周）

#### Sprint 8（第15-16周）：系统测试

##### 第15周：测试准备和执行
- **第1-2天**：测试计划制定
  - 设计测试策略
  - 创建测试用例
  - 准备测试数据

- **第3-5天**：功能测试
  - 执行单元测试
  - 进行集成测试
  - 执行端到端测试

##### 第16周：性能和安全测试
- **第1-2天**：性能测试
  - 执行负载测试
  - 进行压力测试
  - 分析并解决性能问题

- **第3-4天**：安全测试
  - 执行安全漏洞扫描
  - 进行渗透测试
  - 修复安全问题

- **第5天**：Sprint回顾与演示
  - 演示测试结果
  - 讨论剩余问题
  - 规划最终部署

#### Sprint 9（第17-18周）：部署和上线

##### 第17周：部署准备
- **第1-2天**：环境准备
  - 配置生产环境
  - 设置监控系统
  - 准备备份和恢复策略

- **第3-5天**：数据迁移
  - 设计数据迁移策略
  - 执行数据导入
  - 验证数据完整性

##### 第18周：上线和培训
- **第1-2天**：系统部署
  - 执行生产环境部署
  - 进行最终验收测试
  - 确认系统正常运行

- **第3-4天**：用户培训
  - 准备培训材料
  - 进行用户培训
  - 收集用户反馈

- **第5天**：项目总结
  - 总结项目经验
  - 规划后续维护
  - 庆祝项目成功

## 最佳实践实现详解

### 代码质量保证

#### 编码规范
- **命名约定**：使用有意义的驼峰命名法（camelCase）
- **文件组织**：每个文件只包含一个主要组件或功能
- **注释规范**：关键业务逻辑必须有注释说明
- **代码格式化**：使用Prettier自动格式化代码

#### 代码审查流程
- **审查标准**：功能完整性、代码质量、性能考虑、安全性
- **审查工具**：使用GitHub Pull Request进行代码审查
- **自动化检查**：集成ESLint进行静态代码分析
- **审查责任**：每个PR至少需要一名高级开发者审查

#### 持续集成
- **自动化测试**：提交代码时自动运行测试
- **代码覆盖率**：维持至少80%的测试覆盖率
- **构建验证**：确保每次提交都能成功构建
- **性能基准**：监控关键操作的性能变化

### 错误处理与日志

#### 统一错误处理
```typescript
export class AppError extends Error {
  constructor(
    public message: string,
    public statusCode: number = 500,
    public code?: string,
    public details?: any
  ) {
    super(message);
    this.name = 'AppError';
  }

  static badRequest(message: string, details?: any): AppError {
    return new AppError(message, 400, 'BAD_REQUEST', details);
  }

  static notFound(message: string, details?: any): AppError {
    return new AppError(message, 404, 'NOT_FOUND', details);
  }

  static unauthorized(message: string, details?: any): AppError {
    return new AppError(message, 401, 'UNAUTHORIZED', details);
  }

  static forbidden(message: string, details?: any): AppError {
    return new AppError(message, 403, 'FORBIDDEN', details);
  }

  static internal(message: string, details?: any): AppError {
    return new AppError(message, 500, 'INTERNAL_ERROR', details);
  }
}
```

#### 全局错误处理中间件
```typescript
// app/api/middleware.ts
export async function errorHandler(
  request: Request,
  context: { params: Record<string, string> },
  handler: (request: Request, context: { params: Record<string, string> }) => Promise<Response>
): Promise<Response> {
  try {
    return await handler(request, context);
  } catch (error) {
    logger.error('API错误', error as Error);

    if (error instanceof AppError) {
      return Response.json({
        error: {
          message: error.message,
          code: error.code,
          details: error.details
        }
      }, { status: error.statusCode });
    }

    return Response.json({
      error: {
        message: '服务器内部错误',
        code: 'INTERNAL_ERROR'
      }
    }, { status: 500 });
  }
}
```

#### 结构化日志
```typescript
export const logger = {
  info: (message: string, meta?: Record<string, any>) => {
    console.log(JSON.stringify({
      level: 'info',
      message,
      timestamp: new Date().toISOString(),
      ...meta
    }));
  },

  warn: (message: string, meta?: Record<string, any>) => {
    console.warn(JSON.stringify({
      level: 'warn',
      message,
      timestamp: new Date().toISOString(),
      ...meta
    }));
  },

  error: (message: string, error?: Error, meta?: Record<string, any>) => {
    console.error(JSON.stringify({
      level: 'error',
      message,
      error: error ? {
        name: error.name,
        message: error.message,
        stack: error.stack
      } : undefined,
      timestamp: new Date().toISOString(),
      ...meta
    }));
  },

  debug: (message: string, meta?: Record<string, any>) => {
    if (process.env.NODE_ENV !== 'production') {
      console.debug(JSON.stringify({
        level: 'debug',
        message,
        timestamp: new Date().toISOString(),
        ...meta
      }));
    }
  }
};
```

### 测试策略

#### 单元测试
- **测试框架**：Jest
- **测试范围**：所有服务、工具函数和自定义hooks
- **测试原则**：每个函数至少有一个测试用例
- **示例**：
```typescript
// __tests__/services/inventoryService.test.ts
describe('InventoryService', () => {
  let inventoryService: InventoryService;
  let mockInventoryRepository: jest.Mocked<InventoryRepository>;

  beforeEach(() => {
    mockInventoryRepository = {
      findByWarehouseAndProduct: jest.fn(),
      increaseStock: jest.fn(),
      decreaseStock: jest.fn(),
    } as any;

    inventoryService = new InventoryService(
      mockInventoryRepository,
      {} as any,
      {} as any
    );
  });

  describe('transferStock', () => {
    it('当源仓库有足够库存时应该成功转移库存', async () => {
      // 准备测试数据
      mockInventoryRepository.findByWarehouseAndProduct.mockResolvedValueOnce({
        id: 1,
        warehouseId: 1,
        productId: 1,
        quantity: 10
      });

      // 执行测试
      await inventoryService.transferStock(1, 2, 1, 5);

      // 验证结果
      expect(mockInventoryRepository.decreaseStock).toHaveBeenCalledWith(1, 1, 5);
      expect(mockInventoryRepository.increaseStock).toHaveBeenCalledWith(2, 1, 5);
    });

    it('当源仓库库存不足时应该抛出错误', async () => {
      // 准备测试数据
      mockInventoryRepository.findByWarehouseAndProduct.mockResolvedValueOnce({
        id: 1,
        warehouseId: 1,
        productId: 1,
        quantity: 3
      });

      // 执行测试并验证
      await expect(inventoryService.transferStock(1, 2, 1, 5))
        .rejects
        .toThrow('库存不足');
    });
  });
});
```

#### 集成测试
- **测试范围**：API端点、数据库交互
- **测试环境**：使用测试数据库
- **示例**：
```typescript
// __tests__/api/inventory.test.ts
describe('库存API', () => {
  beforeAll(async () => {
    // 设置测试数据库
    await setupTestDatabase();
  });

  afterAll(async () => {
    // 清理测试数据库
    await cleanupTestDatabase();
  });

  it('应该返回指定仓库的库存列表', async () => {
    // 准备测试数据
    await prisma.warehouse.create({
      data: {
        id: 1,
        name: '测试仓库',
        type: 'physical',
        location: '测试地点'
      }
    });

    await prisma.product.create({
      data: {
        id: 1,
        name: '测试产品',
        price: 100
      }
    });

    await prisma.inventoryItem.create({
      data: {
        warehouseId: 1,
        productId: 1,
        quantity: 10
      }
    });

    // 执行测试
    const response = await fetch('/api/inventory?warehouseId=1');
    const data = await response.json();

    // 验证结果
    expect(response.status).toBe(200);
    expect(data.length).toBe(1);
    expect(data[0].productId).toBe(1);
    expect(data[0].quantity).toBe(10);
  });
});
```

#### 端到端测试
- **测试工具**：Playwright
- **测试范围**：关键用户流程
- **示例**：
```typescript
// e2e/inventory.spec.ts
test('库存转移流程', async ({ page }) => {
  // 登录
  await page.goto('/login');
  await page.fill('input[name="email"]', 'admin@example.com');
  await page.fill('input[name="password"]', 'password');
  await page.click('button[type="submit"]');

  // 导航到库存页面
  await page.click('a[href="/inventory"]');

  // 选择源仓库
  await page.selectOption('select[name="sourceWarehouse"]', '1');

  // 选择目标仓库
  await page.selectOption('select[name="targetWarehouse"]', '2');

  // 选择产品
  await page.click('.product-item[data-product-id="1"]');

  // 输入数量
  await page.fill('input[name="quantity"]', '5');

  // 执行转移
  await page.click('button[type="submit"]');

  // 验证成功消息
  await expect(page.locator('.toast-success')).toContainText('库存转移成功');

  // 验证源仓库库存减少
  await page.selectOption('select[name="warehouse"]', '1');
  await expect(page.locator('.product-item[data-product-id="1"] .quantity')).toContainText('5');

  // 验证目标仓库库存增加
  await page.selectOption('select[name="warehouse"]', '2');
  await expect(page.locator('.product-item[data-product-id="1"] .quantity')).toContainText('5');
});
```

### 性能优化策略

#### 数据库优化
- **索引优化**：为常用查询字段创建索引
- **查询优化**：避免N+1查询问题
- **连接池管理**：优化数据库连接池配置
- **批量操作**：使用批量插入和更新

#### 前端性能优化
- **代码分割**：按路由和组件分割代码
- **懒加载**：图片和组件懒加载
- **缓存策略**：使用SWR进行数据缓存
- **静态生成**：可能的情况下使用静态生成

#### API性能优化
- **响应缓存**：缓存不常变化的数据
- **分页处理**：大数据集使用分页
- **压缩**：启用响应压缩
- **限流**：实现API限流保护

## 部署与维护

### 部署流程
1. **准备阶段**
   - 代码冻结和最终审查
   - 创建发布分支
   - 更新版本号和变更日志

2. **构建阶段**
   - 运行完整测试套件
   - 构建生产版本
   - 生成静态资源

3. **数据库迁移**
   - 备份现有数据库
   - 执行迁移脚本
   - 验证数据完整性

4. **部署阶段**
   - 配置环境变量
   - 部署应用服务
   - 更新静态资源
   - 执行健康检查

5. **验证阶段**
   - 执行冒烟测试
   - 监控系统性能
   - 验证关键功能

### 监控与维护

#### 系统监控
- **应用性能监控**：使用New Relic或Datadog
- **错误跟踪**：使用Sentry捕获前端和后端错误
- **日志管理**：集中式日志收集和分析
- **数据库监控**：监控查询性能和连接数

#### 定期维护
- **安全更新**：定期更新依赖包
- **性能审计**：每月进行性能审计
- **数据库维护**：定期优化数据库
- **备份策略**：自动化备份和恢复测试

#### 用户反馈收集
- **反馈渠道**：在应用内提供反馈入口
- **问题跟踪**：使用JIRA管理用户报告的问题
- **用户调研**：定期进行用户满意度调查
- **功能请求**：收集和评估新功能请求

## 风险管理

### 潜在风险识别

#### 技术风险
- **新技术适应风险**：团队对React 19和Next.js 15可能需要适应时间
- **性能风险**：产品图片可视化和多仓库实时预览可能带来性能挑战
- **集成风险**：多模块集成可能导致意外的交互问题

#### 业务风险
- **需求变更风险**：业务需求可能在开发过程中发生变化
- **用户接受度风险**：最终用户可能需要时间适应新系统
- **数据迁移风险**：从旧系统迁移数据可能面临兼容性问题

#### 项目管理风险
- **进度风险**：某些模块可能比预期需要更多时间
- **资源风险**：关键人员可能因故无法参与项目
- **沟通风险**：技术团队与业务团队之间可能存在沟通障碍

### 风险缓解策略

#### 技术风险缓解
- **技术培训**：在项目开始前进行技术栈培训
- **性能测试**：在开发早期进行性能测试和优化
- **架构审查**：定期进行架构审查，确保模块间接口清晰

#### 业务风险缓解
- **需求冻结期**：设定关键阶段的需求冻结期
- **用户参与**：在开发过程中让用户参与测试和反馈
- **数据迁移计划**：制定详细的数据迁移计划和回滚策略

#### 项目管理风险缓解
- **缓冲时间**：在计划中包含缓冲时间
- **知识共享**：确保关键知识在团队内共享
- **沟通计划**：建立清晰的沟通渠道和例会机制

## 成功标准

### 技术成功标准
- **代码质量**：通过静态代码分析工具评估，代码复杂度低于设定阈值
- **测试覆盖率**：单元测试覆盖率达到80%以上
- **性能指标**：页面加载时间小于2秒，API响应时间小于500ms
- **可靠性**：系统正常运行时间达到99.9%

### 业务成功标准
- **用户采纳率**：90%以上的目标用户在上线后一个月内开始使用系统
- **效率提升**：关键业务流程处理时间减少30%
- **错误减少**：人工操作错误减少50%
- **用户满意度**：用户满意度调查得分达到4.0/5.0以上

### 项目管理成功标准
- **进度达成**：项目按计划完成，延期不超过10%
- **预算控制**：项目成本控制在预算范围内
- **范围管理**：所有核心需求得到实现
- **团队满意度**：团队成员对项目过程的满意度达到4.0/5.0以上

## 开发日志与经验总结

### 2025-05-13: 销售模块实现（订单和POS销售）

#### 完成内容
1. **数据库模型扩展**：
   - 创建了Customer、Order和OrderItem模型
   - 扩展了Product模型，添加了更多商品属性
   - 更新了Employee模型，添加了与Order的关系

2. **API实现**：
   - 创建了客户管理API（/api/customers）
   - 创建了订单管理API（/api/orders）
   - 创建了订单项API（/api/orders/[id]/items）
   - 创建了POS销售API（/api/pos）

3. **服务端函数**：
   - 在actions.ts中添加了客户管理相关函数
   - 在actions.ts中添加了订单管理相关函数
   - 在actions.ts中添加了POS销售相关函数

4. **UI组件**：
   - 创建了客户管理组件（customer-management.tsx）
   - 创建了订单管理组件（order-management.tsx）
   - 创建了POS销售组件（pos-system.tsx）

5. **页面实现**：
   - 创建了销售管理页面（/app/sales/page.tsx）
   - 在侧边栏中添加了销售管理入口

#### 实现要点
- 订单销售模式：支持创建订单、管理订单状态、查看订单详情
- POS销售模式：支持快速销售、产品搜索、客户管理
- 库存集成：销售完成后自动减少库存，并记录库存交易
- 客户管理：支持创建、编辑、删除客户，以及客户搜索

#### 遇到的问题与解决方案
1. **问题**：订单完成时需要确保库存足够
   **解决方案**：使用事务（transaction）确保库存检查和减少操作的原子性，如果库存不足则回滚整个事务

2. **问题**：POS销售需要同时支持选择现有客户和创建新客户
   **解决方案**：实现客户选择对话框和新建客户对话框，并支持客户搜索功能

3. **问题**：需要确保订单总金额的准确性
   **解决方案**：在添加或修改订单项时，自动重新计算订单总金额

#### 最佳实践经验
1. **事务处理**：
   - 对于涉及多个数据库操作的功能，使用事务确保数据一致性
   - 特别是在销售过程中，需要同时更新订单、库存和交易记录

2. **用户体验**：
   - POS界面设计注重直观和高效，支持产品图片展示和快速搜索
   - 订单管理界面提供多种筛选选项，便于查找特定订单

3. **代码复用**：
   - 客户选择和创建逻辑在POS和订单模块中复用
   - 库存减少逻辑在订单完成和POS销售中复用

4. **错误处理**：
   - 对所有可能的错误情况进行处理，如库存不足、客户信息不完整等
   - 提供清晰的错误提示，帮助用户理解问题并采取正确的操作

### 2025-05-12: 多仓库库存管理模块实现

#### 完成内容
1. **数据库模型扩展**：创建了Warehouse、InventoryItem和InventoryTransaction模型
2. **数据库迁移**：运行Prisma迁移，将更改应用到数据库
3. **API实现**：
   - 创建了仓库管理API（/api/warehouses）
   - 创建了库存管理API（/api/inventory）
   - 创建了库存转移API（/api/inventory/transfer）
   - 创建了库存交易记录API（/api/inventory/transactions）
4. **服务端函数**：在actions.ts中添加了仓库和库存相关的服务端函数
5. **UI组件**：
   - 创建了仓库管理组件（warehouse-management.tsx）
   - 创建了库存管理组件（inventory-management.tsx）
   - 创建了库存交易记录组件（inventory-transactions.tsx）
6. **页面实现**：创建了库存管理页面（/app/inventory/page.tsx）
7. **导航更新**：在侧边栏中添加了库存管理入口

#### 实现要点
- 使用命令模式处理库存操作，确保库存转移的原子性
- 实现了多仓库管理，支持实体仓库和虚拟仓库
- 库存管理支持产品图片可视化
- 库存转移功能确保源仓库有足够库存
- 库存交易记录支持筛选和分页

#### 遇到的问题与解决方案
1. **问题**：库存转移需要确保原子性操作
   **解决方案**：使用Prisma事务（$transaction）确保库存转移的原子性，避免数据不一致

2. **问题**：需要处理各种错误情况（库存不足、仓库不存在等）
   **解决方案**：创建AppError类，实现统一的错误处理机制，并在API和服务端函数中使用

3. **问题**：库存交易记录需要关联产品和仓库信息
   **解决方案**：在获取交易记录时，同时获取相关的产品和仓库信息，并在前端组合展示

#### 最佳实践经验
1. **事务处理**：
   - 对于涉及多个数据库操作的功能，使用事务确保数据一致性
   - 库存转移同时涉及源仓库减少和目标仓库增加，必须在一个事务中完成

2. **错误处理**：
   - 创建统一的错误处理类，便于管理不同类型的错误
   - 在API和服务端函数中进行详细的错误检查，提供清晰的错误信息

3. **UI设计**：
   - 使用选项卡（Tabs）组织不同的功能模块，提高用户体验
   - 在库存列表中显示产品图片，直观展示库存内容

4. **性能优化**：
   - 库存交易记录使用分页加载，避免一次加载过多数据
   - 使用筛选功能减少数据加载量

### 2025-05-11: 产品管理模块图片可视化功能实现

#### 完成内容
1. **数据库模型扩展**：为Product模型添加了imageUrl和description字段
2. **数据库迁移**：运行Prisma迁移，将更改应用到数据库
3. **API更新**：更新了产品相关的API和actions，以支持图片URL和描述
4. **UI组件更新**：更新了产品设置组件，添加了图片上传和显示功能
5. **导出功能**：添加了产品导出功能，支持导出包含图片URL的Excel文件

#### 实现要点
- 利用现有的FileUpload组件实现图片上传，避免重复代码
- 在产品列表中添加图片列，直观展示产品图片
- 在编辑对话框中添加图片上传区域和描述输入框
- 确保所有API和服务端函数都支持新增字段

#### 遇到的问题与解决方案
1. **问题**：Prisma迁移时需要确保不影响现有数据
   **解决方案**：为新增字段设置为可选（String?），确保向后兼容

2. **问题**：图片上传后需要立即在表单中显示
   **解决方案**：实现handleImageUpload回调函数，在上传完成后更新editingProduct状态

3. **问题**：产品列表中图片可能加载失败
   **解决方案**：添加onError处理，在图片加载失败时显示占位图像

#### 最佳实践经验
1. **数据库变更**：
   - 添加新字段时优先考虑可选字段，避免破坏现有数据
   - 每次变更都创建有意义的迁移名称，便于追踪

2. **组件复用**：
   - 充分利用现有组件（如FileUpload），避免重复代码
   - 确保新功能与现有UI风格保持一致

3. **错误处理**：
   - 为所有可能失败的操作添加适当的错误处理
   - 使用占位图像处理图片加载失败的情况

4. **用户体验**：
   - 在表格中使用缩略图，提高加载速度
   - 提供图片预览和删除功能，方便用户管理

## 结语

本文档详细阐述了聆花文化ERP系统的开发计划和技术实现方案。系统采用最佳实践的编程方式，追求代码量少、低耦合、高复用性和易于排查的实现方案，以满足业务需求的同时确保系统的可维护性和可扩展性。

通过采用敏捷开发方法，结合精益开发原则，项目将在18周内分阶段完成开发和部署。每个Sprint都有明确的目标和可交付成果，确保项目能够持续提供价值。

最终，聆花文化ERP系统将成为一个高效、可靠、易用的综合管理平台，帮助企业实现产品管理、库存管理、销售管理、排班管理、制作管理和渠道管理的数字化转型，提升整体运营效率和决策能力。
